rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to the 'trips' collection and all its items/subcollections
    // This effectively removes the time-based expiration of Test Mode.
    match /trips/{document=**} {
      allow read, write: if true;
    }
  }
}
